# dependencies
node_modules/

# build output
dist/
.astro/

# environment / secrets — never committed (NFR-9)
.env
.env.*
*.pem
credentials

# design source: the Bootstrap Studio project stays outside this repo
*.bsdesign

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# committed template for the env file above (NFR-9: placeholders only)
!.env.example

# Lambda build artefacts (infra/contact) — the zip is rebuilt from source
infra/contact/node_modules/
infra/contact/build/
infra/contact/function.zip
